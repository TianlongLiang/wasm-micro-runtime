# Copyright (C) 2019 Intel Corporation.  All rights reserved.
# SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception

name: compilation on zephyr

on:
  # will be triggered on PR events
  pull_request:
    types:
      - opened
      - synchronize
    paths:
      - ".github/workflows/compilation_on_zephyr.yml"
      - "build-scripts/**"
      - "core/**"
      - "!core/deps/**"
      - "product-mini/platforms/common/**"
      - "product-mini/platforms/zephyr/**"
      - "samples/**"
      - "!samples/workload/**"
      - "tests/wamr-test-suites/**"
      - "wamr-compiler/**"
  # will be triggered on push events
  push:
    branches:
      - main
      - "dev/**"
    paths:
      - ".github/workflows/compilation_on_zephyr.yml"
      - "build-scripts/**"
      - "core/**"
      - "!core/deps/**"
      - "product-mini/platforms/common/**"
      - "product-mini/platforms/zephyr/**"
      - "samples/**"
      - "!samples/workload/**"
      - "tests/wamr-test-suites/**"
      - "wamr-compiler/**"
  # allow to be triggered manually
  workflow_dispatch:

# Cancel any in-flight jobs for the same PR/branch so there's only one active
# at a time
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  # FOR SETUP
  ZEPHYR_SDK_VERSION: "0.16.9"
  ZEPHYR_VERSION: "v3.7.0"
  # FOR BUILD

permissions:
  contents: read

jobs:
  smoke_test:
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        # -- Architecture --
        arch:
          - name: ARC
            board: qemu_arc/qemu_arc_hs
            target: ARC
            toolchains: arc-zephyr-elf:arc64-zephyr-elf
          - name: x86-32
            board: qemu_x86
            target: X86_32
            toolchains: x86_64-zephyr-elf
        # -- Sample variant --
        #
        # `verify` is a space-separated list of stdout strings the runner
        # asserts (logical AND). Each variant asserts banner + a key
        # mid-demo event + completion banner — three points is enough to
        # catch silent corruption without being fragile to interleave order.
        sample:
          - name: simple (kernel-MT)
            path: simple
            extra_cmake: ""
            verify: '"=== simple kernel-mode MT demo:" "[recv] worker" "=== simple demo complete: received 4 msgs ==="'
          - name: user-mode (MT default)
            path: user-mode
            extra_cmake: ""
            verify: '"=== WAMR User-Mode MT + bh_queue Demo ===" "bh_queue created (user mode)" "=== MT Demo complete ==="'
          - name: user-mode (ST)
            path: user-mode
            extra_cmake: "-DUSER_MODE_MULTITHREAD=OFF"
            verify: '"=== WAMR User-Mode ST + bh_queue Demo ===" "bh_queue created (user mode)" "=== ST Demo complete ==="'
          - name: user-mode (MT prebuilt)
            path: user-mode
            extra_cmake: "-DWAMR_USE_PREBUILT_LIB=1"
            verify: '"=== WAMR User-Mode MT + bh_queue Demo ===" "bh_queue created (user mode)" "=== MT Demo complete ==="'
    name: ${{ matrix.sample.name }} (${{ matrix.arch.name }})
    container:
      # For Zephyr 3.7 LTS, use the v0.26-branch or the latest v0.26.x release Docker image.
      # ci require a larger runner to avoid "no space left on device"
      image: ghcr.io/zephyrproject-rtos/ci-base:v0.26-branch
      options: --user root

    steps:
      # https://docs.zephyrproject.org/latest/develop/application/index.html#zephyr-workspace-application
      # zephyrproject/       --> CI ROOT
      # ├─── .west/
      # │    └─── config
      # ├─── bootloader/
      # ├─── zephyr/         --> Zephyr source code
      # ├─── zephyr-sdk/
      # ├─── modules/
      # │    |─── wasm-micro-runtime   --> WAMR source code
      # ├─── tools/
      # ├─── vendor/
      # └─── application/    --> DUMMY. keep west_lite.yml here

      - name: Checkout code
        uses: actions/checkout@v6.0.3
        with:
          path: modules/wasm-micro-runtime

      - name: Prepare Zephyr environment
        shell: bash
        run: |
          mkdir -p application
          cp modules/wasm-micro-runtime/product-mini/platforms/zephyr/simple/west_lite.yml application/west_lite.yml

      - name: Setup Zephyr project
        uses: zephyrproject-rtos/action-zephyr-setup@v1
        with:
          app-path: application
          manifest-file-name: west_lite.yml
          sdk-version: ${{ env.ZEPHYR_SDK_VERSION }}
          toolchains: ${{ matrix.arch.toolchains }}

      - name: Build and run
        shell: bash
        run: |
          pushd product-mini/platforms/zephyr/${{ matrix.sample.path }}
          west build . -b ${{ matrix.arch.board }} -p always -- \
            -DWAMR_BUILD_TARGET=${{ matrix.arch.target }} ${{ matrix.sample.extra_cmake }}
          popd

          # 30s timeout: ARC's QEMU is slower than x86, and the MT
          # variants do 2 workers × 5 messages = 10 round trips plus
          # WASM module execution per worker.
          .github/scripts/run_qemu_and_verify.sh \
            product-mini/platforms/zephyr/${{ matrix.sample.path }}/build 30 ${{ matrix.sample.verify }}
        working-directory: modules/wasm-micro-runtime
